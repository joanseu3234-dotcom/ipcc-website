name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy via Vercel GitHub Integration
        run: echo "Vercel auto-deploys on every push. Site https://ipcc-website.vercel.app"
